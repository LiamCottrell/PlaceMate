<!-- Instead of creating the same template pages but changing the content
      we are utilising Partials. This means we can create the Footers and Headers
      seperately as they will be used for all pages. Then we can include them into the
      page where needed.-->
<!doctype html>
<html lang="en">
<!-- page builder that will pull in partials (prebuilt page sections) and form a full working webpage from that !-->
  <% include ../partials/head %>
  <body>
    <% include ../partials/header %>

<!-- Including this particular partial is unique to this specific page. The content will show
      what was coded in the createPlacement partial page. -->
    <div class="content">
      <div class="spacer">
      </div>
        <% include ../partials/createPlacement %>
    </div>

    <% include ../partials/footer %>

    <% include ../partials/scripts %>
    <!-- pull in page unique scripts !-->
    <script src="js/generic/generic.js"></script>
  </body>
</html>
